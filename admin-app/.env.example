# React Admin App Environment (local development example)
# Do not commit production endpoints or credentials. Override in .env.local when needed.
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_ENVIRONMENT=development
