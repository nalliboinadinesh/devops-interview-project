### Example environment variables for backend
# Do NOT commit real credentials. Use real values in a local `.env` file (gitignored).

# MongoDB connection (example local)
MONGODB_URI=mongodb://localhost:27017/polytechnic-sis

# Server
PORT=5000
NODE_ENV=development

# JWT - required in production (use strong random secrets)
JWT_SECRET=change-me-to-a-strong-secret
JWT_REFRESH_SECRET=change-me-to-a-strong-refresh-secret
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# AWS S3 Configuration (optional - used for file uploads)
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY must be kept secret
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_BUCKET_NAME=your-bucket-name
AWS_REGION=ap-south-1

# Cloudinary (Optional)
CLOUDINARY_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# CORS - comma separated allowed origins for production
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=info
 
# Admin email allowed to login (optional - restrict admin access)
EMAIL_USER=admin@example.com
EMAIL_PASS=your-email-password-or-app-password
